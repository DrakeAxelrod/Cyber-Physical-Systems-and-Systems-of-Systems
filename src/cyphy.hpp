#ifndef CYPHY_HPP
#define CYPHY_HPP
#include <iostream>

// TODO: add functions, structs, etc that we will need for our implementations

/**
 * Print "Hello, group5!"
 *
 * a demonstrative example of a function with this docstring
 * the docstrings should be in the hpp file.
 *
 * @param string does nothing since group5 is hardcoded
 */
void hello(std::string);

#endif
